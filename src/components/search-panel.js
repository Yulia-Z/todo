import React from 'react';
import './search-panel.css';

const SearchPanel = () => {
    return (
        <input type="text"
               className="search-input"
               placeholder="search" />
    )
};

export default SearchPanel;
